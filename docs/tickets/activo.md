# Ticket activo — idle

**Estado:** idle  
**Rama:** `codex/statsedge-ui-polish`

No hay ticket de programación activo. Último cerrado: CAZA-SPARKS-SCROLL-1 (hydrate ventana Caza desde `filtered`; smoke scroll >80 PASS).

## Prompt para Agent chat (copiar tal cual)

```
(idle — espera nuevo ticket del orquestador)
```

## Notas orquestador

- Caza: ventana chart-preview sigue cola de cinta (`filtered`), no `rows` del pipeline.
- Smoke: `firstVisible≈87`, 22 SVG visibles ready; `./vfc` 3210.
- Cola plan: Review RS copy · yield Europa.

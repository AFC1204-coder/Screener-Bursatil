# Ticket activo — idle (IPO-UX-D cerrado; cobertura % limitada)

**Estado:** idle orquestador  
**Rama:** `codex/statsedge-ui-polish`  
**Último cerrado:** IPO-UX-D (columna Desde salida; ancla = chartPreview)  
**Siguiente:** IPO-UX-D2 (campo persistido nocturno, si dueño OK) · IPO-UX-E (ficha) · P2 F

Sin ticket de programación activo.

## Residual D

Mesa real 2026-09-07: filas IPO ≤24 m muestran «— · serie no alcanza salida» porque `chartPreview` ~48 sesiones no cubre `ipoDate` si edad >~2 m. Sin inventar números (correcto). Comparar % útiles → persistir ancla en nocturno (gate datos).

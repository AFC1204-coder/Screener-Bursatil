# Ticket activo — idle

**Estado:** idle  
**Rama:** `codex/statsedge-ui-polish`

No hay ticket de programación activo. Último cerrado: OPS-MINI-SMOKE-1 (Mini tunnel check + Playwright smoke; PASS 525/3578 US).

## Prompt para Agent chat (copiar tal cual)

```
(idle — espera nuevo ticket del orquestador)
```

## Notas orquestador

- `npm run ops:mini-smoke:start` — túnel si hace falta + smoke `:3300`.
- Docs: `docs/ops-mini-smoke.md`. Residual: sparks SVG en smoke (`svgs: 0`); Playwright chromium hay que instalarlo una vez en la máquina.
- Cola plan datos: CAZA-SPARKS-SCROLL-1 · RS Review · yield Europa.
